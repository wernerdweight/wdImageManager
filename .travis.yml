language: php

php:
  - '7.2'
  - '7.3'

script:
  - composer install --ignore-platform-reqs
  - composer phpstan
  - composer ecs
# FIXME: fix warnings and allow this
#  - composer phpmd
  - composer phpunit
  - composer victor show
